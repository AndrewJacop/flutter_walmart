import 'package:flutter/material.dart';

class ListsTap extends StatelessWidget {
  const ListsTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("lists"));
  }
}
