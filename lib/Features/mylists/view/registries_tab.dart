import 'package:flutter/material.dart';

class RegistriesTab extends StatelessWidget {
  const RegistriesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("regis"));
  }
}
